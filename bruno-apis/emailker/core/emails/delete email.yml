info:
  name: delete email
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:3001/api/emails/:id
  params:
    - name: id
      value: UUID_DEL_CORREO
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
