info:
  name: all projects
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3001/api/projects
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
